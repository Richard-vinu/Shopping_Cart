// function strLength(s) {
//     var length = 0;
//     while (s[length] !== undefined){
//       length++;
//     } 
//     return length;
//   }


  
//   console.log(strLength("Hel2lo")); // 5
//   console.log(strLength("")); // 0


//!KEY POINTS 
/*

# When receiving data from a web server, the data is always a string.
Parse the data with JSON.parse(), and the data becomes a JavaScript object.

#findIndex

//cartPresent then CartId is present

let arr = ['apple', 'ball', 'car', ''];

// arr.splice(<index>, <steps>, [elements ...]);

arr.splice(1, 1);			// Removes 1 item at index 1
// => ['foo', 10, 'qux']


What does toFixed 2 do in JavaScript?
The toFixed() method converts a number to a string.
 The toFixed() method rounds the string to a specified number of decimals.




const isValidString = (String) => {
  return /\d/.test(String)}
  console.log(!isValidString('1a'));

The JSON.stringify() method converts a JavaScript value to a JSON string



















